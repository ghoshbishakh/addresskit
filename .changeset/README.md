# Changesets

Changesets track version bumps and changelog entries.

Run `pnpm changeset` to add a new changeset.

Run `pnpm version` to consume changesets and bump versions.

Run `pnpm publish` to publish all bumped packages.
