export {
  createValidator,
  validateAddress,
  validateField,
  normalizeAddress,
  formatAddress,
  lookupPostalCode,
} from "@addresskit/validation";
export type { Validator, ValidatorConfig } from "@addresskit/validation";
